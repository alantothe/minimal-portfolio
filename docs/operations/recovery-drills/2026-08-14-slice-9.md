# Slice 9 encrypted restore drill — 2026-08-14

## Result

Passed.

- Environment: production Docker image built from
  `feature/slice-9-recovery-backups`
- Command: `bun run recovery:fixture-drill`
- Encryption: system `age` 1.1.1, two generated identities; restore used only
  the separate drill identity
- Source: migrated SQLite fixture with immutable Published revisions and four
  referenced Media records
- Publication generation before/after: `6`
- Published fingerprint before/after:
  `ff123a96d51b200898f216eed74a8700ae2727a2e27527a86c0350584b3cb21a`
- Media references before/after: `4`
- Sessions and OAuth attempts invalidated: yes
- Measured duration: `681 ms`

## Checks exercised

- live-safe `VACUUM INTO` export;
- integrity, foreign-key, migration-ledger, revision checksum, route, Media,
  and Blog-view validation;
- manifest and database SHA-256 verification;
- multi-recipient encryption and drill-identity decryption;
- archive path allowlist;
- clean target restore with no copied WAL/SHM files;
- Published generation/fingerprint and Media-reference equality;
- authentication-state invalidation.

## Scope and follow-up

This acceptance drill used the filesystem object-store adapter so no production
content or credentials entered CI. R2 request behavior is covered through the
same object-store interface. After bucket credentials, locks, and lifecycle
rules are installed, run `drill-latest` against the newest real hourly/daily R2
object and record that evidence here. Quarterly operator drill must also cover
Railway snapshot restore and one Cloudinary Media restore.

Next scheduled fixture drill: weekly GitHub Actions run (`17 7 * * 0` UTC).
